# playwright — ENABLED

Browser automation via `@playwright/mcp`.

- Command: `cmd /c npx -y @playwright/mcp@latest`
- Clients: Codex + OpenCode
- Heavy on context; disable here + client config if unused
