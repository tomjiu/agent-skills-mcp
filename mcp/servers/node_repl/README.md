# node_repl — ENABLED

Codex runtime Node REPL / browser bridge binary.

- Command: Codex cua_node `node_repl.exe` (path in client config)
- Clients: Codex + OpenCode (OpenCode points at same runtime path)
- Platform-managed; do not delete runtime folder
